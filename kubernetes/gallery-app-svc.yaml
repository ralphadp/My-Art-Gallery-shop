apiVersion: v1
kind: Service
metadata:
  name: gallery-app-service
  namespace: art
  labels:
    run: gallery-app-service
spec:
  type: NodePort
  ports:
  - port: 3000
    nodePort: 32260
    protocol: TCP
    targetPort: 3000
  selector:
    app: gallery