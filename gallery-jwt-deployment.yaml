apiVersion: apps/v1
kind: Deployment
metadata:
  name: gallery-jwt-deploy
  labels:
    app: gallery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gallery
  template:
    metadata:
      labels:
        app: gallery
    spec:
      containers:
      - image: gallery-jwt:v1
        name: gallery-jwt
        imagePullPolicy: Never
        ports:
        - containerPort: 3333
